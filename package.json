{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "dependencies": {
    "handlebars": "^4.0.5",
    "handlebars-loader": "^1.3.0",
    "http-server": "^0.9.0",
    "ncp": "^2.0.0",
    "node-sass": "^3.8.0",
    "parallelshell": "^2.0.0",
    "webpack": "^1.13.1"
  },
  "devDependencies": {
    "exit-hook": "^1.1.1",
    "front-matter": "^2.1.0",
    "fs-extra": "^0.30.0",
    "globby": "^5.0.0",
    "lodash": "^4.13.1",
    "shelljs": "^0.7.0",
    "tiny-lr": "^0.2.1"
  },
  "scripts": {
    "start": "parallelshell 'webpack --watch' 'node ./watch.js' 'npm run server'",
    "prestart": "webpack && npm run templates && npm run copy && npm run sass",
    "server": "http-server ./dist -c-1",
    "copy": "ncp src/public/ dist/ --stopOnErr",
    "sass": "node-sass -o dist/css -q --output-style compact src/scss/",
    "templates": "node compile-hbs.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
